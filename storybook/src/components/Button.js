import React from 'react'

export const Button = (props) => {
    return (
        <div>
          <button>
              
              {props.title}
              </button>  
        </div>
    )
}
